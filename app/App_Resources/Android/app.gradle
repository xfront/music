// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.github.xfront.music"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
